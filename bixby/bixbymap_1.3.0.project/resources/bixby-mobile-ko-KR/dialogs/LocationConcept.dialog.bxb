 dialog (Result) {
   // bind the variable "rollResult" to the result 
   match {
     LocationConcept (this)
   }
   // define a condition that changes the dialog depending on whether there
   // is one or more dice rolls
  if (size(this) > 1) {
    template("죄송합니다. 못 알아들었습니다.")
  }
}